# Synchronized project content

This directory contains synchronized project content.

Do not edit files here manually.

Canonical content lives in each project repository and arrives here through
the publishing pipeline (`scripts/oss-sync.mjs`, or each project's
`publish-site.yml` workflow). Any manual edit will be overwritten by the next
sync.
