---
title: "Compatibility"
description: "What works where, and why. An honest capability report across browsers, adapters, and hardware."
order: 3
---

Web Bluetooth is not a universal API. It ships in Chromium; it does not ship in Firefox or Safari, and neither vendor is likely to change that soon. This page states plainly what runs where, why the gaps exist, and how you can bridge them yourself if you need to.

<Callout type="warning">
This is a capability report, not a roadmap. Mozilla has declined on the record. Apple has given no signal in 12+ years. Neither is likely to ship native Web Bluetooth in a horizon you can plan against.
</Callout>

## The three adapters

`agnostic-web-ble` ships two adapters and defines a contract for a third:

- **`native`** — wraps `navigator.bluetooth`. Runs anywhere the browser exposes the Web Bluetooth API. In practice: Chromium desktop, Chromium Android, ChromeOS. Reports `requiresUserGesture: true` because `requestDevice()` must be called from a user activation.
- **`mock`** — in-memory adapter for tests and stories. Reports `requiresUserGesture: false`. Never touches hardware. Deterministic, seeded, drive-controllable via `MockDeviceHandle`.
- **`custom`** — anything satisfying the `BluetoothAdapter` interface. This is how you reach Firefox and Safari: build an adapter over a transport your browser *does* support (WebSocket to a local helper, native messaging, whatever) and register it alongside `native`. See [custom adapters](/agnostic-web-ble/docs/custom-adapters).

Selection is deterministic — first available wins. Pass adapters in priority order:

```ts
const bt = createBluetooth({
  adapters: [nativeWebBluetoothAdapter(), myBridgeAdapter()],
});
```

On Chromium the `native` adapter's `isAvailable()` returns `true` and it wins. On Firefox and Safari it returns `false` and the next adapter is tried. If none are available you get `BluetoothError { code: "UNAVAILABLE" }` naming every adapter that was tried along with its reason.

## Matrix

<CompatibilityMatrix
  rows={[
    { runtime: "Chrome / Edge / Opera (desktop)", adapter: "native", request: "Implemented", connect: "Implemented", read: "Implemented", write: "Implemented", notify: "Implemented", note: "Requires HTTPS or localhost." },
    { runtime: "Chrome (Android)", adapter: "native", request: "Implemented", connect: "Implemented", read: "Implemented", write: "Implemented", notify: "Implemented", note: "Since Android 6." },
    { runtime: "ChromeOS", adapter: "native", request: "Implemented", connect: "Implemented", read: "Implemented", write: "Implemented", notify: "Implemented" },
    { runtime: "Firefox (all platforms)", adapter: "native", request: "Not supported", connect: "Not supported", read: "Not supported", write: "Not supported", notify: "Not supported", note: "navigator.bluetooth is undefined. No pref, no flag." },
    { runtime: "Safari (macOS / iOS)", adapter: "native", request: "Not supported", connect: "Not supported", read: "Not supported", write: "Not supported", notify: "Not supported", note: "Same as Firefox. Chrome on iOS inherits WebKit and cannot escape this." },
    { runtime: "Any browser", adapter: "custom (bridge)", request: "You choose", connect: "You choose", read: "You choose", write: "You choose", notify: "You choose", note: "Your transport, your capability report. Be honest to what you built." },
    { runtime: "Node / Vitest / Playwright", adapter: "mock", request: "Implemented", connect: "Implemented", read: "Implemented", write: "Implemented", notify: "Implemented", note: "Zero hardware. requiresUserGesture: false." }
  ]}
/>

## Capability space

<Graph3D
  caption="Browsers reach hardware only through an adapter that runs there. Native reaches Chromium; custom adapters reach everywhere their transport reaches; mock never leaves memory."
  layout="layers"
  nodes={[
    { id: "chrome", label: "Chrome / Edge / Opera", sub: "Chromium", kind: "primary", layer: 0, angle: 0.1 },
    { id: "firefox", label: "Firefox", sub: "Gecko", kind: "secondary", layer: 0, angle: 0.4 },
    { id: "safari", label: "Safari", sub: "WebKit", kind: "secondary", layer: 0, angle: 0.7 },
    { id: "node", label: "Node · Vitest", sub: "no DOM", kind: "faint", layer: 0, angle: 0.95 },
    { id: "native", label: "native", sub: "navigator.bluetooth", kind: "primary", layer: 1, angle: 0.15 },
    { id: "custom", label: "custom", sub: "your transport", kind: "accent", layer: 1, angle: 0.5 },
    { id: "mock", label: "mock", sub: "in-memory", kind: "faint", layer: 1, angle: 0.85 },
    { id: "hw", label: "BLE peripheral", sub: "real GATT", kind: "primary", layer: 2, angle: 0.25 },
    { id: "fixture", label: "fixture", sub: "scripted", kind: "faint", layer: 2, angle: 0.75 }
  ]}
  edges={[
    { from: "chrome", to: "native" },
    { from: "firefox", to: "custom" },
    { from: "safari", to: "custom" },
    { from: "chrome", to: "custom", dashed: true },
    { from: "node", to: "mock" },
    { from: "chrome", to: "mock", dashed: true },
    { from: "native", to: "hw" },
    { from: "custom", to: "hw" },
    { from: "mock", to: "fixture" }
  ]}
/>

The `native` adapter is a thin wrapper over `navigator.bluetooth`. If that global does not exist in a given browser, no version of this library will make it exist. That is not a limitation of `agnostic-web-ble` — it is a property of the browser.

## Why not Firefox and Safari?

**Mozilla — position: harmful.** Mozilla's official standards position on Web Bluetooth is `harmful`. Their concerns are the permission model, the surface area of ambient device access, and the difficulty of meaningful user consent for opaque GATT services. See [mozilla/standards-positions #95](https://github.com/mozilla/standards-positions/issues/95). The Firefox tracking bug, [Bugzilla 674737](https://bugzilla.mozilla.org/show_bug.cgi?id=674737), has sat without implementation work for over a decade. There is no hidden pref, no nightly flag, no partial implementation — `about:config` has no `dom.webbluetooth.enabled`; the code does not exist in Gecko.

**Apple — no signal.** WebKit's tracking bug for Web Bluetooth, [WebKit bug 101034](https://bugs.webkit.org/show_bug.cgi?id=101034), has been open since 2013 with no implementation work and no public position paper. iOS is stricter: every browser on iOS is required to use WebKit, so Chrome and Firefox on iOS inherit Safari's answer.

**The sibling APIs don't help either.** WebSerial, WebHID, WebUSB, and Web NFC follow the same shape as Web Bluetooth: Chromium ships them, Mozilla marks them `harmful`, WebKit does not implement. You can't polyfill Web Bluetooth on top of them in Firefox or Safari because Firefox and Safari don't ship them either. The [WebBluetoothCG implementation status](https://github.com/WebBluetoothCG/web-bluetooth/blob/main/implementation-status.md) tracks this.

State it plainly: on Firefox and Safari, `navigator.bluetooth` is `undefined`, and it will remain `undefined`. Any library that claims otherwise is either lying, requires a browser extension, or is proxying through a native host you install separately.

## Extending to Firefox and Safari yourself

If you control both ends — the web page and the machine it runs on — you can bridge to Firefox and Safari today. Write a small host process that speaks to the OS Bluetooth stack (BlueZ on Linux, CoreBluetooth on macOS, WinRT on Windows) and exposes a WebSocket. Then implement a `BluetoothAdapter` that satisfies the same contract as `native` but talks to your bridge instead of `navigator.bluetooth`. Pass it to `createBluetooth({ adapters: [native, bridge] })` and the library will pick whichever is available at runtime — your code above the adapter layer does not change.

`agnostic-web-ble` does not ship a bridge. Bridges are deployment-specific: authentication, transport, host lifecycle, and OS integration are choices you have to make, and shipping a default would push those choices onto you. The adapter contract is small enough that a working bridge is a weekend, not a quarter.

See [custom adapters](/agnostic-web-ble/docs/custom-adapters) for the full contract and a reference bridge sketch.
